import VehicleStatsListPage from './VehicleStatsListPage'

/** Deep link `/owner/vehicle-stats/:id` opens the same fleet table + drawer. */
const VehicleStatsPage = () => <VehicleStatsListPage />

export default VehicleStatsPage
